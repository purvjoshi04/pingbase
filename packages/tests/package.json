{
  "name": "@pingbase/tests",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@pingbase/api": "workspace:*",
    "@pingbase/store": "workspace:*",
    "@types/bun": "^1.3.13"
  },
  "scripts": {
    "test": "NODE_ENV=test bun --env-file=.env.test test"
  }
}